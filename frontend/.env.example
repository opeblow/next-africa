# Base URL of the NEXT Africa API (no trailing slash).
# In local development the dev server proxies /api to http://localhost:3001,
# but the app calls this absolute URL by default.
VITE_API_URL=http://localhost:3001
